package com.ar_decoration.component;

public class Types {
    public enum DataType {
        NONE,
        MODEL_DATA
    }

    public enum ControlMode {
        NONE,
        MODE_TRANSLATE,
        MODE_ROTATE,
        MODE_SCALE
    }
}
